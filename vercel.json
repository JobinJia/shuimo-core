{
  "buildCommand": "pnpm build && pnpm --filter @shuimo/playground build",
  "outputDirectory": "playground/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
